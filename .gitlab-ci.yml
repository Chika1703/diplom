image: hashicorp/terraform:1.6

stages:
  - validate
  - plan
  - apply

variables:
  TF_IN_AUTOMATION: "true"
  TF_INPUT: "false"

before_script:
  # Переходим в папку с Terraform кодом
  - cd terraform
  - terraform --version

validate:
  stage: validate
  script:
    - terraform init -backend=false
    - terraform validate

plan:
  stage: plan
  script:
    - terraform init
    - terraform plan -out=tfplan
  only:
    - main

apply:
  stage: apply
  script:
    # Применяем план без подтверждения
    - terraform apply -auto-approve tfplan
  only:
    - main
